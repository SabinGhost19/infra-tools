apiVersion: v1
kind: Service
metadata:
  name: oauth2-proxy
  namespace: demo-app
spec:
  type: ExternalName
  externalName: oauth2-proxy.oauth2-proxy.svc.cluster.local
  ports:
    - name: http
      port: 80
      targetPort: 80
