apiVersion: v1
kind: Secret
metadata:
  name: harbor-storage-env-configuration
  namespace: minio-tenant
type: Opaque
stringData:
  config.env: |-
    export MINIO_ROOT_USER="minioadmin"
    export MINIO_ROOT_PASSWORD="MinioConsolePass-2026-Demo"
