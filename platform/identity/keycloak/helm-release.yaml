apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: keycloak-operator
  namespace: platform-identity
spec:
  interval: 10m0s
  chart:
    spec:
      chart: keycloak-operator
      version: "26.*"
      sourceRef:
        kind: HelmRepository
        name: keycloak-operator-oci
        namespace: flux-system
