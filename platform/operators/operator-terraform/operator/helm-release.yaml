apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: tf-controller
  namespace: tf-system
spec:
  interval: 10m0s
  timeout: 15m0s
  install:
    remediation:
      retries: 3
  upgrade:
    remediation:
      retries: 3
  chart:
    spec:
      chart: tofu-controller
      version: "0.16.2"
      sourceRef:
        kind: HelmRepository
        name: tf-controller
        namespace: flux-system
  values:
    awsPackage:
      install: false
    replicaCount: 1
    watchAllNamespaces: true
    allowCrossNamespaceRefs: true
    logEncoding: console
    logLevel: info
    runner:
      grpcMaxMessageSize: 20
      image:
        pullPolicy: IfNotPresent
